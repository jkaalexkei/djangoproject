// window.addEventListener('scroll',function(){
//     let animacion = this.document.getElementById('subtitulo');
//     let posicionobj = animacion.getBoundingClientRect().top;//se obtiene la posicion al hacer scroll

//     let tamagnopantalla = window.innerHeight; //se obtiene tamaño de pantalla
    
//     if (posicionobj < tamagnopantalla){
//         animacion.style.animation = 'moverin 1s ease-in';
//     }

// })

// window.addEventListener('scroll',function(){
//     let animacion = this.document.getElementById('nosotros');
//     let posicionobj = animacion.getBoundingClientRect().top;//se obtiene la posicion al hacer scroll

//     let tamagnopantalla = window.innerHeight; //se obtiene tamaño de pantalla
    
//     if (posicionobj < tamagnopantalla){
//         animacion.style.animation = 'moverout ease-in-out 1s';
//     }

// })

// window.addEventListener('scroll',function(){
//     let animacion = this.document.getElementById('imagenobj');
//     let posicionobj = animacion.getBoundingClientRect().top;//se obtiene la posicion al hacer scroll

//     let tamagnopantalla = window.innerHeight / 2.5; //se obtiene tamaño de pantalla
    
//     if (posicionobj < tamagnopantalla){
//         animacion.style.animation = 'moverout ease-in 1s';
//     }

// })